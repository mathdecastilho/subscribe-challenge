services:
  app:
    build: .
    # Runs the CLI. Pass the basket as the argument:
    #   docker compose run app "1 music CD at 14.99"

  test:
    build: .
    entrypoint: bundle exec rspec
    # Runs the full test suite:
    #   docker compose run test
